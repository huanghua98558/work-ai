# 环境变量示例文件
# 复制此文件为 .env 并填写实际值
# 注意：不要将 .env 文件提交到版本控制系统

# Node 环境
NODE_ENV=development

# 服务器配置
PORT=5000

# 数据库配置
# 格式：postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://username:password@localhost:5432/workbot

# JWT 配置
# JWT 密钥（至少 32 个字符，生产环境请使用强密码）
JWT_SECRET=your-jwt-secret-key-at-least-32-characters-long-for-security

# JWT 访问令牌过期时间（默认 24 小时）
JWT_ACCESS_EXPIRES_IN=24h

# JWT 刷新令牌过期时间（默认 7 天）
JWT_REFRESH_EXPIRES_IN=7d

# WebSocket 配置
# 心跳间隔（毫秒）
WS_HEARTBEAT_INTERVAL=30000

# 最大 WebSocket 连接数
WS_MAX_CONNECTIONS=1000

# 告警配置（可选）
ALERT_ENABLED=false
ALERT_WEBHOOK_URL=https://example.com/webhook
ALERT_EMAIL=admin@example.com

# CORS 配置（可选）
CORS_ORIGIN=*

# API 限流配置
# 启用限流
RATE_LIMIT_ENABLED=true

# 限流时间窗口（毫秒）
RATE_LIMIT_WINDOW_MS=60000

# 每个时间窗口内的最大请求数
RATE_LIMIT_MAX_REQUESTS=100

# 生产环境注意事项：
# 1. 使用强密码的 DATABASE_URL
# 2. 使用至少 32 个字符的 JWT_SECRET
# 3. 不要使用默认的 JWT_SECRET
# 4. 确保数据库使用 SSL 连接（生产环境）
# 5. 根据实际需求调整 RATE_LIMIT 配置
