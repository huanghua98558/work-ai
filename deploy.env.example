# WorkBot 部署环境变量配置示例
# 请根据实际部署平台的要求配置以下环境变量

# ============================================
# 数据库配置 (必需)
# ============================================

# 主数据库连接 URL（PostgreSQL）
# 部署平台可能使用不同的环境变量名称：
# - DATABASE_URL (推荐)
# - POSTGRES_URL
# - POSTGRESQL_URL
# - DB_URL
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# ============================================
# 安全配置 (必需)
# ============================================

# JWT 密钥（用于生成和验证 JWT token）
# 请使用强随机字符串，至少 32 个字符
JWT_SECRET=your-secret-key-at-least-32-characters-long-change-this-in-production

# ============================================
# 应用配置 (可选)
# ============================================

# 运行环境 (development | production)
NODE_ENV=production

# 服务端口 (默认: 5000)
PORT=5000

# ============================================
# 企业微信配置 (可选)
# ============================================

# 企业微信 API 配置
WEWORK_CORP_ID=your-corp-id
WEWORK_AGENT_ID=your-agent-id
WEWORK_SECRET=your-secret

# ============================================
# 微信公众号配置 (可选)
# ============================================

# 微信公众号 API 配置
WECHAT_APP_ID=your-app-id
WECHAT_APP_SECRET=your-app-secret

# ============================================
# 知识库配置 (可选)
# ============================================

# 知识库存储配置
KNOWLEDGE_BASE_PATH=/tmp/knowledge

# ============================================
# 日志配置 (可选)
# ============================================

# 日志级别 (debug | info | warn | error)
LOG_LEVEL=info

# 日志文件路径
LOG_PATH=/app/work/logs/bypass

# ============================================
# 监控配置 (可选)
# ============================================

# 健康检查间隔 (秒)
HEALTH_CHECK_INTERVAL=30

# WebSocket 最大连接数
WS_MAX_CONNECTIONS=100
